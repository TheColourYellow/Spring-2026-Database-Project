spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        ddl-auto: validate

  application:
    name: project
  datasource:
    url: jdbc:mariadb://localhost:3306/project_example_db_dump_populated
    username: testuser
    password: password